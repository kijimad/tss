{
  "name": "k8s-sim",
  "version": "1.0.0",
  "description": "Kubernetes simulator — Pod scheduling, ReplicaSet, Service, Node scaling",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^6.0.2",
    "vite": "^8.0.2",
    "vitest": "^4.1.1"
  }
}
