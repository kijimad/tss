{
  "name": "natgw-sim",
  "version": "1.0.0",
  "description": "AWS NAT Gateway simulator — SNAT、コネクション追跡、ポートマッピング、帯域・同時接続制限",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^6.0.2",
    "vite": "^8.0.2",
    "vitest": "^4.1.1"
  }
}
