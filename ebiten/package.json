{
  "name": "ebiten-sim",
  "version": "1.0.0",
  "description": "Ebiten game engine simulator — Game loop, Image, GeoM, Input, Audio, Shader",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^6.0.2",
    "vite": "^8.0.2",
    "vitest": "^4.1.1"
  }
}
