{
  "name": "linking-sim",
  "version": "1.0.0",
  "description": "Static / Dynamic linker simulator — symbol resolution, GOT/PLT, lazy binding",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^6.0.2",
    "vite": "^8.0.2",
    "vitest": "^4.1.1"
  }
}
