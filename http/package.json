{
  "name": "http-sim",
  "version": "1.0.0",
  "description": "TCP/IP + HTTP スタック シミュレータ (TypeScript / ブラウザ)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "jsdom": "^29.0.1",
    "typescript": "^6.0.2",
    "vite": "^8.0.2",
    "vitest": "^4.1.1"
  }
}
