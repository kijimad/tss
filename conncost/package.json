{
  "name": "conncost-sim",
  "version": "1.0.0",
  "description": "Connection cost simulator — TCP/TLS/QUIC handshake cost, keep-alive, multiplexing, 0-RTT",
  "type": "module",
  "scripts": { "dev": "vite", "build": "vite build", "test": "vitest run", "test:watch": "vitest" },
  "devDependencies": { "typescript": "^6.0.2", "vite": "^8.0.2", "vitest": "^4.1.1" }
}
